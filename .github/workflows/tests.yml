name: Run tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions: {}

jobs:
  tests:
    uses: fizyk/actions-reuse/.github/workflows/shared-tests-pytests.yml@ef34949dba56816730c78752529724b4aa96e557 # v5.8.0
    with:
      python-versions: '["3.10", "3.11", "3.12", "3.13", "3.14", "3.15", "pypy-3.11"]'
      dependency-manager: 'uv'
      pytest_opts: '-n auto --dist loadgroup'
    secrets:
      codecov_token: ${{ secrets.CODECOV_TOKEN }}
  macostests:
    uses: fizyk/actions-reuse/.github/workflows/shared-tests-pytests.yml@ef34949dba56816730c78752529724b4aa96e557 # v5.8.0
    needs: [tests]
    with:
      python-versions: '["3.12", "3.13", "3.14", "3.15", "pypy-3.11"]'
      dependency-manager: 'uv'
      pytest_opts: '-n auto --dist loadgroup --basetemp=$RUNNER_TEMP/pt'
      os: macos-14 # macos-latest is on macos-15 which misbehaves'
    secrets:
      codecov_token: ${{ secrets.CODECOV_TOKEN }}
  oldest:
    needs: [tests]
    runs-on: 'ubuntu-latest'
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14", "3.15", "pypy-3.11"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up UV on python ${{ matrix.python-version }}
        uses: fizyk/actions-reuse/.github/actions/uv-setup@ef34949dba56816730c78752529724b4aa96e557 # v5.8.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: false
      - name: Install oldest supported versions
        uses: fizyk/actions-reuse/.github/actions/uv-run@ef34949dba56816730c78752529724b4aa96e557 # v5.8.0
        with:
          command: pip install -r oldest-requirements.rq
      - name: Run tests
        uses: fizyk/actions-reuse/.github/actions/uv-run@ef34949dba56816730c78752529724b4aa96e557 # v5.8.0
        with:
          command: pytest -v --cov --cov-report=xml -n auto --dist loadgroup
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@303a32d7a59b442fa8d48b6a1cc6825c09c847a5 # v7.1.1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: unittests
          env_vars: OS,PYTHON
          fail_ci_if_error: false
