name: Run tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  tests:
    uses: fizyk/actions-reuse/.github/workflows/shared-tests-pytests.yml@3768ca868776ab12e78334cc376094a7aec3d9b6 # v5.3.1
    with:
      python-versions: '["3.10", "3.11", "3.12", "3.13", "3.14", "3.15", "pypy-3.11"]'
      dependency-manager: 'uv'
      pytest_opts: '-n auto --dist loadgroup'
    secrets:
      codecov_token: ${{ secrets.CODECOV_TOKEN }}
  macostests:
    uses: fizyk/actions-reuse/.github/workflows/shared-tests-pytests.yml@3768ca868776ab12e78334cc376094a7aec3d9b6 # v5.3.1
    needs: [tests]
    with:
      python-versions: '["3.12", "3.13", "3.14", "3.15", "pypy-3.11"]'
      dependency-manager: 'uv'
      pytest_opts: '-n auto --dist loadgroup --basetemp=$RUNNER_TEMP/pt'
      os: macos-14 # macos-latest is on macos-15 which misbehaves'
    secrets:
      codecov_token: ${{ secrets.CODECOV_TOKEN }}
  oldest:
    needs: [tests]
    runs-on: 'ubuntu-latest'
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14", "3.15", "pypy-3.11"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up UV on python ${{ matrix.python-version }}
        uses: fizyk/actions-reuse/.github/actions/uv-setup@31be9bf07155154cfce850e9d4133b502ee6f023 # v5.3.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: false
      - name: Install oldest supported versions
        uses: fizyk/actions-reuse/.github/actions/uv-run@3768ca868776ab12e78334cc376094a7aec3d9b6 # v5.3.1
        with:
          command: pip install -r oldest-requirements.rq
      - name: Run tests
        uses: fizyk/actions-reuse/.github/actions/uv-run@3768ca868776ab12e78334cc376094a7aec3d9b6 # v5.3.1
        with:
          command: pytest -v --cov --cov-report=xml -n auto --dist loadgroup
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: unittests
          env_vars: OS,PYTHON
          fail_ci_if_error: false
